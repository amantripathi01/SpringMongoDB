# ********RoostGPT********

# Test generated by RoostGPT for test amanPostman16 using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  environments:
    production:
      target: "https://api.production.com"
    staging:
      target: "https://api.staging.com"
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "id"
      - "type"
      - "value"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Availability Check Scenario"
    flow:
      - log: "Starting Availability Check request"
      - post:
          url: "/availabilityCheck"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            content-type: "application/json"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ id }}"
              complement:
                complements:
                  - type: "{{ type }}"
                    value: "{{ value }}"
      - capture:
          header: "Content-Type"
          as: "Response_ContentType"
      - expect:
          - statusCode: 200
          - hasHeader: "Content-Type"
          - headerEquals: 
              header: "Content-Type"
              value: "application/json"
          - hasProperty: "control.type"
          - hasProperty: "control.message"
          - hasProperty: "control.code"
          - hasProperty: "resource.inventoryId"
          - hasProperty: "resource.maxBandwidth"
          - hasProperty: "resource.availabilityType"
          - hasProperty: "resource.availabilityCode"
      - think: 1
      - log: "Completed Availability Check request"
