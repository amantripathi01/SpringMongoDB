# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country_get for http method type GET in artillery framework
# RoostTestHash=3d9e7cb5d1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests Scenario"
    flow:
      - log: "Testing invalid authentication headers"
      - get:
          url: "/country"
          headers:
            app-identifier: "invalid-app-identifier"
            customer-secret-key: "invalid-customer-secret-key"
      - expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing missing authentication headers"
      - get:
          url: "/country"
          headers:
            app-identifier: ""
            customer-secret-key: ""
      - expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing invalid URL"
      - get:
          url: "/invalid-country"
          headers:
            app-identifier: "{{ $processEnvironment.AUTH_TOKEN }}"
            customer-secret-key: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing valid headers but expecting non-successful response"
      - get:
          url: "/country"
          headers:
            app-identifier: "{{ $processEnvironment.AUTH_TOKEN }}"
            customer-secret-key: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - notStatusCode: 200
          - hasHeader: "Content-Type"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2
