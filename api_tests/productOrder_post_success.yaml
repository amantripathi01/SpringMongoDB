# ********RoostGPT********

# Test generated by RoostGPT for test amanBedrock using AI Type Vertex AI and AI Model claude-3-5-sonnet-v2
# 
# Test generated for /productOrder_post for http method type POST in artillery framework
# RoostTestHash=d2b09bfc44
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 1
      arrivalRate: 1

scenarios:
  - name: "Test successful product order creation"
    flow:
      - log:
          message: "Starting product order creation test"
      
      - post:
          url: "/productOrder"
          expect:
            - statusCode: 201
            - contentType: "application/json"
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - hasProperty: "order"
            - hasProperty: "order.id"
      
      - think:
          duration: 1
