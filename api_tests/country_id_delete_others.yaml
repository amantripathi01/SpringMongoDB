# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=5c040001f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_id_delete_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests for Country Deletion"
    flow:
      - log: "Attempting to delete a country with an invalid ID"
      - delete:
          url: "/country/invalid-id"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 400
          hasHeader: "Content-Type"
          hasProperty: "error"
          hasProperty: "message"
      - think: 2

      - log: "Attempting to delete a country without authentication"
      - delete:
          url: "/country/32"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
      - expect:
          statusCode: 401
          hasHeader: "Content-Type"
          hasProperty: "error"
          hasProperty: "message"
      - think: 2

      - log: "Attempting to delete a country with an invalid app-identifier"
      - delete:
          url: "/country/32"
          headers:
            app-identifier: "invalid-app-identifier"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 403
          hasHeader: "Content-Type"
          hasProperty: "error"
          hasProperty: "message"
      - think: 2

      - log: "Attempting to delete a country with an invalid customer-secret-key"
      - delete:
          url: "/country/32"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "invalid-customer-secret-key"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 403
          hasHeader: "Content-Type"
          hasProperty: "error"
          hasProperty: "message"
      - think: 2

      - log: "Attempting to delete a country with a non-existent ID"
      - delete:
          url: "/country/999999"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 404
          hasHeader: "Content-Type"
          hasProperty: "error"
          hasProperty: "message"
      - think: 2
