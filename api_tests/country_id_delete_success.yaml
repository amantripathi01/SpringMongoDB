# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=5c040001f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_id_delete_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Delete Country by ID - Successful Requests"
    flow:
      - log: "Starting scenario to delete a country by ID with valid headers and path parameters."
      - log: "Sending DELETE request to /country/{id} with valid ID and headers."
      - delete:
          url: "/country/{{ id }}"
          headers:
            app-identifier: "d645d2b40811f5672c93794d8622da45588f32cdb2ed85336475bf479e5d"
            customer-secret-key: "12546580645c21b8a3529680f455f715b6b6829da8976bdfd2e9d51181fe"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasProperty: "status"
      - expect:
          hasProperty: "message"
      - expect:
          hasProperty: "error_info"
      - expect:
          equals:
            status: "success"
      - expect:
          equals:
            message: "Country deleted successfully"
      - expect:
          equals:
            error_info: null
      - think: 2
