# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country_post for http method type POST in artillery framework
# RoostTestHash=b16b61c2ee
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_post_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Create Country API Test"
    flow:
      - log: "Starting test for creating a new country with valid payload."
      - post:
          url: "/country"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            app-identifier: "d645d2b40811f5672c93794d8622da45588f32cdb2ed85336475bf479e5d"
            customer-secret-key: "12546580645c21b8a3529680f455f715b6b6829da8976bdfd2e9d51181fe"
          json:
            name: "{{ name }}"
            code: "{{ code }}"
            currencies: "{{ currencies }}"
            default_currency: "{{ default_currency }}"
            is_active: "{{ is_active }}"
      - expect:
          statusCode: 201
          contentType: "application/json"
          hasProperty: "status"
          hasProperty: "data"
          hasProperty: "data.id"
          hasProperty: "data.name"
          hasProperty: "data.code"
          hasProperty: "data.default_currency"
          hasProperty: "data.is_active"
          hasProperty: "data.app_id"
          hasProperty: "data.customer_id"
          hasProperty: "data.created_by"
          hasProperty: "data.updated_by"
          hasProperty: "data.created_at"
          hasProperty: "data.updated_at"
          hasProperty: "data.currencies"
          equals:
            - status: "success"
            - message: "Country created successfully"
          headerEquals:
            Content-Type: "application/json; charset=utf-8"
      - think: 2
