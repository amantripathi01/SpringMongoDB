# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country/{id}_get for http method type GET in artillery framework
# RoostTestHash=9f2a876166
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_id_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests for Country Endpoint"
    flow:
      - log: "Testing with invalid country ID (non-integer)"
      - get:
          url: "/country/abc"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
      - expect:
          - statusCode: 400
          - hasHeader: "Content-Type"
          - contentType: "application/json"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing with missing app-identifier header"
      - get:
          url: "/country/32"
          headers:
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
      - expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - contentType: "application/json"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing with invalid URL"
      - get:
          url: "/country/invalid-url"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
      - expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - contentType: "application/json"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing with missing customer-secret-key header"
      - get:
          url: "/country/32"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
      - expect:
          - statusCode: 401
          - hasHeader: "Content-Type"
          - contentType: "application/json"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing with non-existent country ID"
      - get:
          url: "/country/999999"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
      - expect:
          - statusCode: 404
          - hasHeader: "Content-Type"
          - contentType: "application/json"
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2
