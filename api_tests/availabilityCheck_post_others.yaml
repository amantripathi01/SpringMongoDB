# ********RoostGPT********

# Test generated by RoostGPT for test amanArtilery using AI Type Azure Open AI and AI Model Inference
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=11e354dac0
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_others.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "field1"
      - "field2"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Request 1: Invalid URL"
      - post:
          url: "/invalidEndpoint"
          payload:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404
          - contentType: json
          - hasProperty: error
          - equals:
              - property: error
              - value: "Not Found"
      - think: 2

      - log: "Request 2: Invalid Payload"
      - post:
          url: "/availabilityCheck"
          payload:
            invalidField: "invalidValue"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 400
          - contentType: json
          - hasProperty: error
          - equals:
              - property: error
              - value: "Bad Request"
      - think: 2

      - log: "Request 3: Missing Authentication"
      - post:
          url: "/availabilityCheck"
          payload:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 401
          - contentType: json
          - hasProperty: error
          - equals:
              - property: error
              - value: "Unauthorized"
      - think: 2

      - log: "Request 4: Invalid Authentication"
      - post:
          url: "/availabilityCheck"
          payload:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalidToken"
        expect:
          - statusCode: 403
          - contentType: json
          - hasProperty: error
          - equals:
              - property: error
              - value: "Forbidden"
      - think: 2
