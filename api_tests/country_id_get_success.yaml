# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country/{id}_get for http method type GET in artillery framework
# RoostTestHash=9f2a876166
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: country_id_get_success.csv
    skipHeader: true
    delimiter: "^|^"
  variables:
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"
    appIdentifier: "d645d2b40811f5672c93794d8622da45588f32cdb2ed85336475bf479e5d"
    customerSecretKey: "12546580645c21b8a3529680f455f715b6b6829da8976bdfd2e9d51181fe"

scenarios:
  - name: "Get Country by ID - Success Scenario"
    flow:
      - log: "Starting test: Retrieve country details by ID with valid headers and path parameters."
      - get:
          url: "/country/{{ id }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
            app-identifier: "{{ appIdentifier }}"
            customer-secret-key: "{{ customerSecretKey }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasHeader: "Content-Type"
      - expect:
          hasProperty: "status"
      - expect:
          hasProperty: "message"
      - expect:
          hasProperty: "data"
      - expect:
          hasProperty: "data.country"
      - expect:
          hasProperty: "data.country.id"
      - expect:
          hasProperty: "data.country.name"
      - expect:
          hasProperty: "data.country.code"
      - expect:
          hasProperty: "data.country.default_currency"
      - expect:
          hasProperty: "data.country.is_active"
      - expect:
          hasProperty: "data.country.app_id"
      - expect:
          hasProperty: "data.country.customer_id"
      - expect:
          hasProperty: "data.country.created_by"
      - expect:
          hasProperty: "data.country.updated_by"
      - expect:
          hasProperty: "data.country.created_at"
      - expect:
          hasProperty: "data.country.updated_at"
      - expect:
          hasProperty: "data.country.currencies"
      - expect:
          equals:
            path: "status"
            value: "success"
      - expect:
          equals:
            path: "message"
            value: "Data fetched successfully"
      - expect:
          matchesRegexp:
            path: "data.country.created_at"
            pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.000Z$"
      - expect:
          matchesRegexp:
            path: "data.country.updated_at"
            pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.000Z$"
      - think: 2
