# ********RoostGPT********

# Test generated by RoostGPT for test postman-collection using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /chat/completions_post for http method type POST in artillery framework
# RoostTestHash=44d8c175ca
# 
# 

# ********RoostGPT********
config:
  targets:
    - target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    source: "chat_completions_post_success.csv"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: Successful API Response Validation for /chat/completions
    flow:
      - log: "Starting test scenario for '/chat/completions' endpoint with valid payload."
      - post:
          url: "/chat/completions"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          payload:
            model: "{{ model }}"
            messages:
              - role: "{{ role }}"
                content: "{{ content }}"
            temperature: "{{ temperature }}"
            max_tokens: "{{ max_tokens }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasProperty: "id"
      - expect:
          hasProperty: "choices"
      - think:
          duration: 2
