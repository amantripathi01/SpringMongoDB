# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country/{id}_put for http method type PUT in artillery framework
# RoostTestHash=e5eb5af926
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_id_put_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Update Country by ID - Success Scenario"
    flow:
      - log: "Starting test for updating country by ID with valid payload."
      - post:
          url: "/country/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
          json:
            name: "{{ name }}"
            code: "{{ code }}"
            currencies: "{{ currencies }}"
            default_currency: "{{ default_currency }}"
            is_active: "{{ is_active }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty: "status"
      - expect:
          hasProperty: "message"
      - expect:
          hasProperty: "data"
      - expect:
          hasProperty: "error_info"
      - expect:
          equals:
            status: "success"
      - expect:
          equals:
            message: "Country updated successfully"
      - expect:
          hasProperty:
            data.id: "{{ id }}"
      - expect:
          hasProperty:
            data.name: "{{ name }}"
      - expect:
          hasProperty:
            data.code: "{{ code }}"
      - expect:
          hasProperty:
            data.default_currency: "{{ default_currency }}"
      - expect:
          hasProperty:
            data.is_active: "{{ is_active }}"
      - expect:
          hasProperty:
            data.app_id: "{{ app_id }}"
      - expect:
          hasProperty:
            data.customer_id: "{{ customer_id }}"
      - expect:
          hasProperty:
            data.created_by: "{{ created_by }}"
      - expect:
          hasProperty:
            data.updated_by: "{{ updated_by }}"
      - expect:
          hasProperty:
            data.created_at: "{{ created_at }}"
      - expect:
          hasProperty:
            data.updated_at: "{{ updated_at }}"
      - expect:
          hasProperty:
            data.currencies: "{{ currencies }}"
      - think: 2
