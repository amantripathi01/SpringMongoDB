# ********RoostGPT********

# Test generated by RoostGPT for test defaultIteration using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /country_get for http method type GET in artillery framework
# RoostTestHash=3d9e7cb5d1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "country_get_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Validate Successful API Responses for Country Endpoint"
    flow:
      - log: "Initiating request to fetch all countries with valid headers."
      - get:
          url: "/country"
          headers:
            app-identifier: "{{ $processEnvironment.APP_IDENTIFIER }}"
            customer-secret-key: "{{ $processEnvironment.CUSTOMER_SECRET_KEY }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty: "status"
      - expect:
          hasProperty: "message"
      - expect:
          hasProperty: "data"
      - expect:
          hasProperty: "data.countries"
      - expect:
          jmespath: "status == 'success'"
      - expect:
          jmespath: "message == 'Data fetched successfully'"
      - expect:
          jmespath: "data.countries[0].id"
      - expect:
          jmespath: "data.countries[0].name"
      - expect:
          jmespath: "data.countries[0].code"
      - expect:
          jmespath: "data.countries[0].default_currency"
      - expect:
          jmespath: "data.countries[0].is_active"
      - expect:
          jmespath: "data.countries[0].app_id"
      - expect:
          jmespath: "data.countries[0].customer_id"
      - expect:
          jmespath: "data.countries[0].created_by"
      - expect:
          jmespath: "data.countries[0].updated_by"
      - expect:
          jmespath: "data.countries[0].created_at"
      - expect:
          jmespath: "data.countries[0].updated_at"
      - expect:
          jmespath: "data.countries[0].currencies"
      - think: 2
