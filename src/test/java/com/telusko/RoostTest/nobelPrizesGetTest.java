// Test generated by RoostGPT for test aman-1Dec using AI Type Open AI and AI Model gpt-4

// Test generated for /nobelPrizes_get for http method type GET in rest-assured framework

// RoostTestHash=4940f5c55b

package com.telusko.RoostTest;

import io.restassured.RestAssured;
import io.restassured.response.Response;
import org.junit.Test;
import org.junit.jupiter.api.DisplayName;
import static io.restassured.RestAssured.given;
import static org.hamcrest.Matchers.*;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.util.HashMap;
import java.util.Map;

public class NobelPrizesGetTest {

    @Test
    @DisplayName("Nobel Prizes Get Test")
    public void nobelPrizes_get_Test() {
        RestAssured.baseURI = System.getenv("BASE_URL");

        try (BufferedReader reader = new BufferedReader(new FileReader("src/test/java/com/telusko/RoostTest/nobelPrizes_get.csv"))) {
            String headerLine = reader.readLine();
            String[] headers = headerLine.split(",");

            String line;
            while ((line = reader.readLine()) != null) {
                String[] data = line.split(",");

                Map<String, String> map = new HashMap<>();
                for (int i = 0; i < headers.length; i++) {
                    map.put(headers[i], data[i]);
                }

                Response response = given()
                        .contentType(ContentType.JSON)
                        .pathParams(map)
                        .when()
                        .get("/nobelPrizes")
                        .then()
                        .extract().response();

                switch (response.statusCode()) {
                    case 200:
                        validateSuccessResponse(response);
                        break;
                    case 400:
                        validateErrorResponse(response);
                        break;
                    case 404:
                        validateNotFoundResponse(response);
                        break;
                    case 422:
                        validateUnprocessableEntityResponse(response);
                        break;
                    default:
                        fail("Invalid response status.");
                }
            }
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    private void validateSuccessResponse(Response response) {
        // Validate the successful response
        assertNotNull(response.jsonPath().get("nobelPrizes"));
    }

    private void validateErrorResponse(Response response) {
        // Validate the error response
        assertEquals("Bad request", response.jsonPath().get("message"));
    }

    private void validateNotFoundResponse(Response response) {
        // Validate the Not Found response
        assertEquals("Not Found", response.jsonPath().get("message"));
    }

    private void validateUnprocessableEntityResponse(Response response) {
        // Validate the Unprocessable Entity response
        assertEquals("Unprocessable Entity", response.jsonPath().get("message"));
    }
}
