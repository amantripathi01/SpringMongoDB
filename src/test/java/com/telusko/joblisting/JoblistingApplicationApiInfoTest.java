
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanVertex2 using AI Type Open AI and AI Model chatgpt-4o-latest

Test generated by RoostGPT for test amanVertex2 using AI Type Open AI and AI Model chatgpt-4o-latest

ROOST_METHOD_HASH=apiInfo_b15996cb2b
ROOST_METHOD_SIG_HASH=apiInfo_95b025c193

Here are the test scenarios for the `apiInfo` method in the `JoblistingApplication` class:

---

### **Scenario 1: Verify ApiInfo Object Creation**

**Details:**
- **TestName:** apiInfoReturnsNonNullApiInfo
- **Description:** This test ensures that the `apiInfo` method returns a non-null `ApiInfo` object. Since the method initializes an `ApiInfoBuilder` and calls `build()`, it should always return a valid `ApiInfo` instance.

**Execution:**
- **Arrange:** No specific setup is required as the method does not take any parameters.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Verify that the returned `ApiInfo` object is not null.

**Validation:**
- The assertion ensures that the method correctly initializes and returns an `ApiInfo` object.
- This is crucial because a null return value could cause failures in dependent components such as Swagger configuration.

---

### **Scenario 2: Verify ApiInfo Object Has Default Values**

**Details:**
- **TestName:** apiInfoReturnsApiInfoWithDefaultValues
- **Description:** This test checks whether the `ApiInfo` object returned by `apiInfo` contains default values, as the `ApiInfoBuilder` is used without setting any properties.

**Execution:**
- **Arrange:** No setup is required.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Verify that the returned `ApiInfo` object has default values (e.g., empty title, description, version, etc.).

**Validation:**
- Ensures that the `ApiInfoBuilder` does not introduce unexpected default values.
- Confirms that the method does not set any specific metadata, which might indicate missing configurations.

---

### **Scenario 3: Ensure ApiInfo Instance is Consistently Created**

**Details:**
- **TestName:** apiInfoReturnsNewInstanceEachTime
- **Description:** This test ensures that each invocation of `apiInfo` returns a new `ApiInfo` instance rather than reusing a cached or singleton instance.

**Execution:**
- **Arrange:** No setup is required.
- **Act:** Invoke `apiInfo` twice and store the results.
- **Assert:** Verify that the two returned `ApiInfo` instances are different objects (i.e., they have different memory references).

**Validation:**
- Ensures that the method does not return a shared instance, which could lead to unintended modifications in a multi-threaded environment.
- Confirms that the method correctly creates a new `ApiInfo` object each time it is called.

---

### **Scenario 4: Validate Integration with Docket Configuration**

**Details:**
- **TestName:** apiInfoIsUsedInDocketConfiguration
- **Description:** This test verifies that the `apiInfo` method is correctly used within the `Docket` bean configuration. The `Docket` object should reference the `ApiInfo` instance returned by `apiInfo()`.

**Execution:**
- **Arrange:** Instantiate the `JoblistingApplication` class.
- **Act:** Invoke the `api` method and retrieve the `ApiInfo` from the returned `Docket` instance.
- **Assert:** Verify that the `ApiInfo` object in `Docket` is the same as the one returned by `apiInfo()`.

**Validation:**
- Ensures that the Swagger configuration correctly integrates the `ApiInfo` instance.
- Confirms that the `apiInfo` method is effectively contributing to the API documentation setup.

---

### **Scenario 5: Ensure ApiInfoBuilder is Used Correctly**

**Details:**
- **TestName:** apiInfoUsesApiInfoBuilderCorrectly
- **Description:** This test verifies that the `ApiInfoBuilder` is used correctly within the `apiInfo` method and that calling `build()` does not result in an exception.

**Execution:**
- **Arrange:** No setup is required.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Ensure that no exceptions are thrown during execution.

**Validation:**
- Ensures that the `ApiInfoBuilder` is correctly instantiated and used.
- Confirms that calling `build()` does not result in an error, which could indicate an issue with the builder configuration.

---

These test scenarios comprehensively cover the `apiInfo` method, ensuring correctness, consistency, and integration with the `Docket` configuration.
*/

// ********RoostGPT********
package com.telusko.joblisting;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.service.ApiInfo;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

@SpringBootTest
class JoblistingApplicationApiInfoTest {

	private final JoblistingApplication joblistingApplication = new JoblistingApplication();

	@Test
	@Tag("valid")
	public void apiInfoReturnsNonNullApiInfo() {
		ApiInfo apiInfo = joblistingApplication.apiInfo();
		assertNotNull(apiInfo, "ApiInfo should not be null");
	}

	@Test
	@Tag("valid")
	public void apiInfoReturnsNewInstanceEachTime() {
		ApiInfo firstInstance = joblistingApplication.apiInfo();
		ApiInfo secondInstance = joblistingApplication.apiInfo();
		assertNotSame(firstInstance, secondInstance, "Each call to apiInfo should return a new instance");
	}

	@Test
	@Tag("valid")
	public void apiInfoUsesApiInfoBuilderCorrectly() {
		assertDoesNotThrow(() -> joblistingApplication.apiInfo(),
				"ApiInfoBuilder should be used correctly without exceptions");
	}

	@Test
	@Tag("integration")
	public void apiInfoIsUsedInDocketConfiguration() {
		Docket docket = joblistingApplication.api();
		ApiInfo expectedApiInfo = joblistingApplication.apiInfo();
		assertNotNull(docket, "Docket should not be null");
		assertEquals(expectedApiInfo.getClass(), docket.getDocumentationType().getClass(),
				"Docket should use the same ApiInfo type as apiInfo()");
	}

}