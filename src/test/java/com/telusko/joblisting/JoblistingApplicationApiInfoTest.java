
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanDeepSeek using AI Type deepseek_ai and AI Model deepseek-reasoner

Test generated by RoostGPT for test amanDeepSeek using AI Type deepseek_ai and AI Model deepseek-reasoner

ROOST_METHOD_HASH=apiInfo_b15996cb2b
ROOST_METHOD_SIG_HASH=apiInfo_95b025c193

```
  Scenario 1: apiInfo method returns non-null ApiInfo instance

  Details:
    TestName: apiInfoReturnsNonNullApiInfo
    Description: Verify that the apiInfo() method returns a non-null ApiInfo object when invoked. This ensures the Spring Bean is properly initialized for Swagger documentation.
  Execution:
    Arrange: Initialize the JoblistingApplication class context to access beans.
    Act: Invoke the apiInfo() method through the application context.
    Assert: Use assertNotNull() to validate the returned ApiInfo instance is not null.
  Validation:
    Ensures the ApiInfo bean is correctly initialized by Spring. A null value would break Swagger configuration and prevent API documentation generation, making this a foundational validation.

  Scenario 2: apiInfo contains empty default title field

  Details:
    TestName: apiInfoHasEmptyDefaultTitle
    Description: Validate that the ApiInfo object created by apiInfo() has an empty/default title field when no custom configuration is provided.
  Execution:
    Arrange: Obtain the ApiInfo instance from the apiInfo() method.
    Act: Access the title property of the ApiInfo object.
    Assert: Assert that the title matches the expected default (empty string or null based on ApiInfoBuilder behavior).
  Validation:
    Confirms the title field adheres to Springfox defaults. This is critical because Swagger UI displays this title, and incorrect values could indicate misconfiguration.

  Scenario 3: apiInfo uses default version value from builder

  Details:
    TestName: apiInfoVersionMatchesBuilderDefault
    Description: Check that the version field in ApiInfo matches the default value set by ApiInfoBuilder when no explicit version is configured.
  Execution:
    Arrange: Retrieve the ApiInfo object via the apiInfo() method.
    Act: Extract the version value using getVersion().
    Assert: Assert equality against the ApiInfoBuilder's default version (e.g., "1.0" or empty string).
  Validation:
    Validates that the API version metadata follows expected conventions. Incorrect versions could mislead API consumers about endpoint compatibility.

  Scenario 4: apiInfo termsOfServiceUrl is unconfigured

  Details:
    TestName: apiInfoTermsOfServiceUrlIsNotSet
    Description: Ensure the termsOfServiceUrl field remains unset/null when no value is provided to ApiInfoBuilder.
  Execution:
    Arrange: Get the ApiInfo instance from the apiInfo() method.
    Act: Retrieve the termsOfServiceUrl property.
    Assert: Verify the value is null or an empty string using JUnit assertions.
  Validation:
    Ensures no unintended terms of service URL is propagated to Swagger documentation. This field is optional and should only appear if explicitly configured.

  Scenario 5: apiInfo contact metadata uses empty defaults

  Details:
    TestName: apiInfoContactHasNoDefaults
    Description: Confirm that contact information fields (name, email, URL) are empty/null by default when not explicitly set in ApiInfoBuilder.
  Execution:
    Arrange: Acquire the ApiInfo object via the apiInfo() method.
    Act: Access the Contact object and its properties through getContact().
    Assert: Assert all contact properties (name, email, URL) are null/empty.
  Validation:
    Validates that contact metadata doesn’t contain placeholder values. Erroneous contact details could misdirect users attempting to reach API maintainers.

  Scenario 6: apiInfo license info remains uninitialized

  Details:
    TestName: apiInfoLicenseInfoIsEmptyByDefault
    Description: Verify that license-related fields (license name and URL) are empty/null when not configured in ApiInfoBuilder.
  Execution:
    Arrange: Obtain the ApiInfo instance from the apiInfo() method.
    Act: Retrieve license name and URL via getLicense() and getLicenseUrl().
    Assert: Check both fields are null or empty strings.
  Validation:
    Ensures license metadata isn’t populated with defaults accidentally. Correct license info is legally required for public APIs but should only be set intentionally.
```,
*/

// ********RoostGPT********

package com.telusko.joblisting;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import springfox.documentation.service.ApiInfo;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

public class JoblistingApplicationApiInfoTest {

	@Test
	@Tag("valid")
	public void apiInfoReturnsNonNullApiInfo() {
		JoblistingApplication app = new JoblistingApplication();
		ApiInfo apiInfo = app.apiInfo();
		assertNotNull(apiInfo);
	}

	@Test
	@Tag("valid")
	public void apiInfoHasEmptyDefaultTitle() {
		JoblistingApplication app = new JoblistingApplication();
		ApiInfo apiInfo = app.apiInfo();
		assertEquals("", apiInfo.getTitle());
	}

	@Test
	@Tag("valid")
	public void apiInfoVersionMatchesBuilderDefault() {
		JoblistingApplication app = new JoblistingApplication();
		ApiInfo apiInfo = app.apiInfo();
		assertEquals("", apiInfo.getVersion());
	}

	@Test
	@Tag("valid")
	public void apiInfoTermsOfServiceUrlIsNotSet() {
		JoblistingApplication app = new JoblistingApplication();
		ApiInfo apiInfo = app.apiInfo();
		assertEquals("", apiInfo.getTermsOfServiceUrl());
	}

	@Test
	@Tag("valid")
	public void apiInfoContactHasNoDefaults() {
		JoblistingApplication app = new JoblistingApplication();
		ApiInfo apiInfo = app.apiInfo();
		assertNull(apiInfo.getContact());
	}

	@Test
	@Tag("valid")
	public void apiInfoLicenseInfoIsEmptyByDefault() {
		JoblistingApplication app = new JoblistingApplication();
		ApiInfo apiInfo = app.apiInfo();
		assertEquals("", apiInfo.getLicense());
		assertEquals("", apiInfo.getLicenseUrl());
	}

}