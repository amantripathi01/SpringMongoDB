
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanDeepSeek using AI Type deepseek_ai and AI Model deepseek-chat

Test generated by RoostGPT for test amanDeepSeek using AI Type deepseek_ai and AI Model deepseek-chat

ROOST_METHOD_HASH=apiInfo_b15996cb2b
ROOST_METHOD_SIG_HASH=apiInfo_95b025c193

Here are the test scenarios for the `apiInfo()` method in the `JoblistingApplication` class:

---

**Scenario 1**: Verify that `apiInfo()` returns a non-null `ApiInfo` object

**Details**:
- **TestName**: apiInfoReturnsNonNullApiInfo
- **Description**: Ensures that the `apiInfo()` method returns a valid `ApiInfo` object and not `null`.

**Execution**:
- **Arrange**: No setup required as the method does not depend on external state.
- **Act**: Call `apiInfo()`.
- **Assert**: Assert that the returned `ApiInfo` object is not `null`.

**Validation**:
- The assertion ensures that the method successfully constructs an `ApiInfo` object.
- This is important because Swagger documentation generation relies on a valid `ApiInfo` object.

---

**Scenario 2**: Verify that `apiInfo()` returns an `ApiInfo` object with default values

**Details**:
- **TestName**: apiInfoReturnsDefaultValues
- **Description**: Checks if the `ApiInfo` object returned by `apiInfo()` contains default values (empty or null fields) since no custom values are set in the builder.

**Execution**:
- **Arrange**: No setup required.
- **Act**: Call `apiInfo()`.
- **Assert**: Assert that fields like `title`, `description`, `version`, etc., in the returned `ApiInfo` object are either `null` or empty.

**Validation**:
- Validates that the `ApiInfoBuilder` does not set any default metadata if not explicitly configured.
- Ensures that Swagger UI does not display incorrect or unintended API metadata.

---

**Scenario 3**: Verify that `apiInfo()` does not throw exceptions during execution

**Details**:
- **TestName**: apiInfoDoesNotThrowException
- **Description**: Ensures that the method executes without throwing any exceptions.

**Execution**:
- **Arrange**: No setup required.
- **Act**: Call `apiInfo()`.
- **Assert**: Assert that no exception is thrown during execution.

**Validation**:
- Confirms that the method is stable and does not fail unexpectedly.
- Important for ensuring that the application starts correctly without runtime errors in Swagger configuration.

---

**Scenario 4**: Verify that `apiInfo()` returns a new instance on each invocation

**Details**:
- **TestName**: apiInfoReturnsNewInstanceEachTime
- **Description**: Checks if multiple calls to `apiInfo()` return different `ApiInfo` instances.

**Execution**:
- **Arrange**: No setup required.
- **Act**: Call `apiInfo()` twice and store the results.
- **Assert**: Assert that the two returned `ApiInfo` objects are not the same instance.

**Validation**:
- Ensures that the method behaves as a proper Spring `@Bean` and does not reuse the same instance.
- Prevents unintended side effects if the `ApiInfo` object is modified elsewhere.

---

**Scenario 5**: Verify that `apiInfo()` works when integrated with `Docket`

**Details**:
- **TestName**: apiInfoWorksWithDocket
- **Description**: Ensures that the `ApiInfo` object returned by `apiInfo()` can be successfully passed to `Docket` without errors.

**Execution**:
- **Arrange**: No setup required.
- **Act**: Call `api()` (which internally uses `apiInfo()`) and retrieve the `Docket` object.
- **Assert**: Assert that `Docket` is initialized without errors and contains the `ApiInfo` object.

**Validation**:
- Validates that the `apiInfo()` method is compatible with Swagger's `Docket` configuration.
- Ensures that the Swagger documentation is generated correctly.

---

These scenarios cover basic functionality, edge cases, and integration checks for the `apiInfo()` method. Let me know if you need additional scenarios!,
*/

// ********RoostGPT********

package com.telusko.joblisting;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.service.ApiInfo;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

@SpringBootTest
class JoblistingApplicationApiInfoTest {

	private final JoblistingApplication joblistingApplication = new JoblistingApplication();

	@Test
	@Tag("valid")
	public void testApiInfoReturnsNonNullApiInfo() {
		ApiInfo apiInfo = joblistingApplication.apiInfo();
		assertNotNull(apiInfo, "ApiInfo should not be null");
	}

	@Test
	@Tag("valid")
	public void testApiInfoReturnsDefaultValues() {
		ApiInfo apiInfo = joblistingApplication.apiInfo();
		assertNull(apiInfo.getTitle(), "Title should be null by default");
		assertNull(apiInfo.getDescription(), "Description should be null by default");
		assertNull(apiInfo.getVersion(), "Version should be null by default");
	}

	@Test
	@Tag("valid")
	public void testApiInfoDoesNotThrowException() {
		assertDoesNotThrow(() -> joblistingApplication.apiInfo(), "apiInfo() should not throw any exceptions");
	}

	@Test
	@Tag("valid")
	public void testApiInfoReturnsNewInstanceEachTime() {
		ApiInfo firstInstance = joblistingApplication.apiInfo();
		ApiInfo secondInstance = joblistingApplication.apiInfo();
		assertNotSame(firstInstance, secondInstance, "Each call to apiInfo() should return a new instance");
	}

	@Test
	@Tag("integration")
	public void testApiInfoWorksWithDocket() {
		assertDoesNotThrow(() -> joblistingApplication.api(),
				"api() should work with ApiInfo without throwing exceptions");
	}

}