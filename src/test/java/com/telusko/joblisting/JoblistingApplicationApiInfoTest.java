
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanVertex2 using AI Type Open AI and AI Model chatgpt-4o-latest

Test generated by RoostGPT for test amanVertex2 using AI Type Open AI and AI Model chatgpt-4o-latest

ROOST_METHOD_HASH=apiInfo_b15996cb2b
ROOST_METHOD_SIG_HASH=apiInfo_95b025c193

Here are the test scenarios for the `apiInfo` method in the `JoblistingApplication` class:

---

### **Scenario 1: Verify ApiInfo Object Creation**

**Details:**
- **TestName:** apiInfoShouldReturnNonNullApiInfo
- **Description:** This test ensures that the `apiInfo` method returns a non-null `ApiInfo` object. Since the method uses `ApiInfoBuilder` to construct the object, it should always return a valid instance.

**Execution:**
- **Arrange:** No specific setup is required.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Verify that the returned object is not null.

**Validation:**
- The assertion ensures that the `apiInfo` method correctly initializes and returns an `ApiInfo` object.
- This test is significant because a null return value would indicate a misconfiguration in the Swagger setup, potentially breaking API documentation.

---

### **Scenario 2: Verify ApiInfo Object Is Empty**

**Details:**
- **TestName:** apiInfoShouldReturnEmptyApiInfo
- **Description:** This test checks that the `ApiInfo` object returned by `apiInfo` does not contain any pre-set values. Since the `ApiInfoBuilder` is used without setting any properties, the resulting `ApiInfo` should be empty.

**Execution:**
- **Arrange:** No specific setup is required.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Verify that the returned `ApiInfo` object has empty or default values for its properties.

**Validation:**
- This test ensures that the `apiInfo` method does not introduce unintended default values.
- It is important because incorrect or unexpected values in the `ApiInfo` object could mislead API consumers.

---

### **Scenario 3: Ensure ApiInfo Is Used in Docket Configuration**

**Details:**
- **TestName:** apiInfoShouldBeUsedInDocketConfiguration
- **Description:** This test verifies that the `apiInfo` method is correctly used in the `Docket` bean configuration. Since the `Docket` bean explicitly calls `apiInfo()`, it should receive the same `ApiInfo` instance.

**Execution:**
- **Arrange:** Instantiate the `JoblistingApplication` class.
- **Act:** Retrieve the `Docket` bean and extract its `ApiInfo` configuration.
- **Assert:** Verify that the `ApiInfo` instance in `Docket` is the same as the one returned by `apiInfo()`.

**Validation:**
- This test ensures that the Swagger configuration correctly integrates the `ApiInfo` object.
- If the `Docket` bean does not use the expected `ApiInfo`, it could lead to missing or incorrect API documentation.

---

### **Scenario 4: Validate ApiInfoBuilder Usage**

**Details:**
- **TestName:** apiInfoShouldUseApiInfoBuilder
- **Description:** This test ensures that the `apiInfo` method correctly utilizes `ApiInfoBuilder` to construct the `ApiInfo` object.

**Execution:**
- **Arrange:** No specific setup is required.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Verify that the returned `ApiInfo` object was created using `ApiInfoBuilder`.

**Validation:**
- This test confirms that the method follows the intended design pattern for constructing `ApiInfo`.
- If `ApiInfoBuilder` is not used, the method might not be extensible for future enhancements.

---

### **Scenario 5: Ensure ApiInfo Method Does Not Throw Exceptions**

**Details:**
- **TestName:** apiInfoShouldNotThrowException
- **Description:** This test ensures that calling the `apiInfo` method does not result in any exceptions. Since the method only initializes an `ApiInfoBuilder` and calls `build()`, it should not fail under normal conditions.

**Execution:**
- **Arrange:** No specific setup is required.
- **Act:** Invoke the `apiInfo` method.
- **Assert:** Ensure that no exceptions are thrown during execution.

**Validation:**
- This test guarantees that the method is robust and does not fail unexpectedly.
- If an exception occurs, it could indicate an issue with the Swagger dependency or an incorrect configuration.

---

These test scenarios comprehensively cover the `apiInfo` method, ensuring its correctness, integration, and robustness. 🚀
*/

// ********RoostGPT********
package com.telusko.joblisting;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.service.ApiInfo;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

@SpringBootTest
class JoblistingApplicationApiInfoTest {

	private final JoblistingApplication joblistingApplication = new JoblistingApplication();

	@Test
	@Tag("valid")
	public void apiInfoShouldReturnNonNullApiInfo() {
		ApiInfo apiInfo = joblistingApplication.apiInfo();
		assertNotNull(apiInfo, "ApiInfo should not be null");
	}

	@Test
	@Tag("valid")
	public void apiInfoShouldReturnEmptyApiInfo() {
		ApiInfo apiInfo = joblistingApplication.apiInfo();
		assertNull(apiInfo.getTitle(), "ApiInfo title should be null");
		assertNull(apiInfo.getDescription(), "ApiInfo description should be null");
		assertNull(apiInfo.getVersion(), "ApiInfo version should be null");
	}

	@Test
	@Tag("valid")
	public void apiInfoShouldUseApiInfoBuilder() {
		ApiInfo apiInfo = joblistingApplication.apiInfo();
		ApiInfo expectedApiInfo = new ApiInfoBuilder().build();
		assertEquals(expectedApiInfo, apiInfo, "ApiInfo should be created using ApiInfoBuilder");
	}

	@Test
	@Tag("valid")
	public void apiInfoShouldNotThrowException() {
		assertDoesNotThrow(() -> joblistingApplication.apiInfo(), "apiInfo method should not throw an exception");
	}

}