//This test file is marked invalid as it contains compilation errors. Change the extension to of this file to .java, to manually edit its contents


// ********RoostGPT********
/*
Test generated by RoostGPT for test amanFileConnector using AI Type Vertex AI and AI Model code-bison

ROOST_METHOD_HASH=getExp_a80bcda041
ROOST_METHOD_SIG_HASH=getExp_e90c52dc7e

 **Scenario 1: Get Experience - Positive Case**

**TestName**: getExp_ReturnsExperience_WhenExperienceIsSet()

**Description**: This test verifies that the getExp() method successfully retrieves the experience value when it has been set for a Post object.

**Execution**:
- Arrange: Create a Post object and set its experience (exp) field to a specific value, such as 5 years.
- Act: Call the getExp() method on the Post object.
- Assert: Use JUnit's assertEquals() method to compare the returned experience value with the expected value of 5 years.

**Validation**: This test ensures that the getExp() method correctly retrieves the experience information stored in the Post object, which is crucial for displaying or processing experience-related data.

**Scenario 2: Get Experience - Negative Case**

**TestName**: getExp_ReturnsZero_WhenExperienceNotSet()

**Description**: This test checks that the getExp() method returns a default value, such as 0, when the experience field has not been explicitly set for a Post object.

**Execution**:
- Arrange: Create a Post object without setting the experience field.
- Act: Call the getExp() method on the Post object.
- Assert: Use JUnit's assertEquals() method to verify that the returned experience value is 0.

**Validation**: This test ensures that the getExp() method handles the case where experience information is missing by returning a default value, preventing unexpected behavior or errors in downstream logic.

**Scenario 3: Get Experience - Boundary Case**

**TestName**: getExp_ReturnsExperience_WhenExperienceIsZero()

**Description**: This test verifies that the getExp() method correctly handles boundary conditions by returning the experience value even when it is set to 0.

**Execution**:
- Arrange: Create a Post object and set its experience field to 0.
- Act: Call the getExp() method on the Post object.
- Assert: Use JUnit's assertEquals() method to confirm that the returned experience value is 0.

**Validation**: This test ensures that the getExp() method works correctly even when the experience value is 0, ensuring consistent behavior across different scenarios.

**Scenario 4: Get Experience - Null Value Handling**

**TestName**: getExp_ReturnsZero_WhenExperienceIsNull()

**Description**: This test checks that the getExp() method gracefully handles null values by returning a default value, such as 0, when the experience field is null.

**Execution**:
- Arrange: Create a Post object and set its experience field to null.
- Act: Call the getExp() method on the Post object.
- Assert: Use JUnit's assertEquals() method to verify that the returned experience value is 0.

**Validation**: This test ensures that the getExp() method can handle null values without causing errors or unexpected behavior, maintaining the integrity of the application's logic.
*/

// ********RoostGPT********
package com.telusko.joblisting.model;import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.*;
import org.springframework.data.mongodb.core.mapping.Document;
import java.util.Arrays;

public class PostGetExpTest {
    @Test
    @DisplayName("getExp_ReturnsExperience_WhenExperienceIsSet")
    @Tag("valid")
    void getExp_ReturnsExperience_WhenExperienceIsSet() {
        // Arrange
        Post post = new Post();
        post.setExp(5);
        // Act
        int experience = post.getExp();
        // Assert
        assertEquals(5, experience);
    }
    @Test
    @DisplayName("getExp_ReturnsZero_WhenExperienceNotSet")
    @Tag("invalid")
    void getExp_ReturnsZero_WhenExperienceNotSet() {
        // Arrange
        Post post = new Post();
        // Act
        int experience = post.getExp();
        // Assert
        assertEquals(0, experience);
    }
    @Test
    @DisplayName("getExp_ReturnsExperience_WhenExperienceIsZero")
    @Tag("boundary")
    void getExp_ReturnsExperience_WhenExperienceIsZero() {
        // Arrange
        Post post = new Post();
        post.setExp(0);
        // Act
        int experience = post.getExp();
        // Assert
        assertEquals(0, experience);
    }
    @Test
    @DisplayName("getExp_ReturnsZero_WhenExperienceIsNull")
    @Tag("valid")
    void getExp_ReturnsZero_WhenExperienceIsNull() {
        // Arrange
        Post post = new Post();
        post.setExp(null);
        // Act
        int experience = post.getExp();
        // Assert
        assertEquals(0, experience);
    }
}