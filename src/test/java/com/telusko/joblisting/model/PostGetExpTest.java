
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanVertex2 using AI Type Open AI and AI Model chatgpt-4o-latest

Test generated by RoostGPT for test amanVertex2 using AI Type Open AI and AI Model chatgpt-4o-latest

ROOST_METHOD_HASH=getExp_a80bcda041
ROOST_METHOD_SIG_HASH=getExp_e90c52dc7e

Here are multiple JUnit test scenarios for the `getExp` method in the `Post` class:

---

### **Scenario 1**: Retrieve Default Experience Value

**Details:**
  - **TestName:** getExpReturnsDefaultValue
  - **Description:** This test verifies that when a `Post` object is instantiated without setting the `exp` field, calling `getExp()` returns the default value of `exp`. Since `exp` is an `int` and not explicitly initialized, it should default to `0`.

**Execution:**
  - **Arrange:** Create a new instance of the `Post` class.
  - **Act:** Call the `getExp()` method.
  - **Assert:** Verify that the returned value is `0`.

**Validation:**
  - Ensures that the `exp` field defaults to `0` when not explicitly set.
  - Confirms that the method correctly retrieves the default value of an uninitialized `int` field.

---

### **Scenario 2**: Retrieve Experience After Setting a Positive Value

**Details:**
  - **TestName:** getExpReturnsSetPositiveValue
  - **Description:** This test checks if `getExp()` correctly returns a positive experience value after it has been explicitly set using `setExp(int exp)`.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `5` using `setExp(5)`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `5`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field.
  - Confirms that `getExp()` retrieves the updated value.

---

### **Scenario 3**: Retrieve Experience After Setting Zero

**Details:**
  - **TestName:** getExpReturnsZeroWhenSetToZero
  - **Description:** This test ensures that if `exp` is explicitly set to `0`, `getExp()` correctly returns `0`.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `0` using `setExp(0)`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `0`.

**Validation:**
  - Ensures that `setExp(0)` correctly updates the `exp` field.
  - Confirms that `getExp()` retrieves `0` when explicitly set.

---

### **Scenario 4**: Retrieve Experience After Setting a Negative Value

**Details:**
  - **TestName:** getExpReturnsNegativeValue
  - **Description:** This test verifies that `getExp()` correctly returns a negative experience value after it has been explicitly set using `setExp(int exp)`.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `-3` using `setExp(-3)`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `-3`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field with a negative value.
  - Confirms that `getExp()` retrieves the updated negative value.

---

### **Scenario 5**: Retrieve Experience After Setting a Large Positive Value

**Details:**
  - **TestName:** getExpHandlesLargePositiveValue
  - **Description:** This test verifies that `getExp()` correctly handles and returns a large positive experience value.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `1000000` using `setExp(1000000)`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `1000000`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field with a large value.
  - Confirms that `getExp()` retrieves the updated large value without overflow or truncation.

---

### **Scenario 6**: Retrieve Experience After Multiple Updates

**Details:**
  - **TestName:** getExpReturnsLatestUpdatedValue
  - **Description:** This test ensures that `getExp()` always returns the latest value after multiple updates to the `exp` field.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `2`, then update it to `10`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `10`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field multiple times.
  - Confirms that `getExp()` retrieves the most recent value.

---

### **Scenario 7**: Retrieve Experience After Setting Minimum Integer Value

**Details:**
  - **TestName:** getExpHandlesMinIntegerValue
  - **Description:** This test verifies that `getExp()` correctly handles and returns `Integer.MIN_VALUE`.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `Integer.MIN_VALUE`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `Integer.MIN_VALUE`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field with the minimum integer value.
  - Confirms that `getExp()` retrieves the correct value without underflow.

---

### **Scenario 8**: Retrieve Experience After Setting Maximum Integer Value

**Details:**
  - **TestName:** getExpHandlesMaxIntegerValue
  - **Description:** This test verifies that `getExp()` correctly handles and returns `Integer.MAX_VALUE`.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `Integer.MAX_VALUE`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `Integer.MAX_VALUE`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field with the maximum integer value.
  - Confirms that `getExp()` retrieves the correct value without overflow.

---

### **Scenario 9**: Retrieve Experience After Setting and Resetting to Default

**Details:**
  - **TestName:** getExpResetsToZeroAfterExplicitReset
  - **Description:** This test ensures that after setting `exp` to a non-default value and then resetting it to `0`, `getExp()` correctly returns `0`.

**Execution:**
  - **Arrange:** Create a `Post` object, set `exp` to `7`, then reset it to `0`.
  - **Act:** Call `getExp()`.
  - **Assert:** Verify that the returned value is `0`.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field multiple times.
  - Confirms that `getExp()` retrieves `0` after an explicit reset.

---

### **Scenario 10**: Retrieve Experience After Setting a Value and Calling `toString()`

**Details:**
  - **TestName:** getExpValueMatchesToStringOutput
  - **Description:** This test verifies that after setting `exp` to a specific value, the `toString()` method correctly reflects the same value.

**Execution:**
  - **Arrange:** Create a `Post` object and set `exp` to `8`.
  - **Act:** Call `getExp()` and `toString()`.
  - **Assert:** Verify that the returned value from `getExp()` matches the `exp` value in the `toString()` output.

**Validation:**
  - Ensures that `setExp(int exp)` correctly updates the `exp` field.
  - Confirms that `toString()` correctly includes the `exp` value.

---

These test scenarios cover various edge cases, including default values, positive and negative values, boundary values, and interactions with other methods.
*/

// ********RoostGPT********

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.data.mongodb.core.mapping.Document;
import java.util.Arrays;

public class PostGetExpTest {

	@Test
	@Tag("valid")
	public void getExpReturnsDefaultValue() {
		// Arrange
		Post post = new Post();
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(0, result);
	}

	@Test
	@Tag("valid")
	public void getExpReturnsSetPositiveValue() {
		// Arrange
		Post post = new Post();
		post.setExp(5);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(5, result);
	}

	@Test
	@Tag("valid")
	public void getExpReturnsZeroWhenSetToZero() {
		// Arrange
		Post post = new Post();
		post.setExp(0);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(0, result);
	}

	@Test
	@Tag("valid")
	public void getExpReturnsNegativeValue() {
		// Arrange
		Post post = new Post();
		post.setExp(-3);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(-3, result);
	}

	@Test
	@Tag("boundary")
	public void getExpHandlesLargePositiveValue() {
		// Arrange
		Post post = new Post();
		post.setExp(1000000);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(1000000, result);
	}

	@Test
	@Tag("valid")
	public void getExpReturnsLatestUpdatedValue() {
		// Arrange
		Post post = new Post();
		post.setExp(2);
		post.setExp(10);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(10, result);
	}

	@Test
	@Tag("boundary")
	public void getExpHandlesMinIntegerValue() {
		// Arrange
		Post post = new Post();
		post.setExp(Integer.MIN_VALUE);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(Integer.MIN_VALUE, result);
	}

	@Test
	@Tag("boundary")
	public void getExpHandlesMaxIntegerValue() {
		// Arrange
		Post post = new Post();
		post.setExp(Integer.MAX_VALUE);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(Integer.MAX_VALUE, result);
	}

	@Test
	@Tag("valid")
	public void getExpResetsToZeroAfterExplicitReset() {
		// Arrange
		Post post = new Post();
		post.setExp(7);
		post.setExp(0);
		// Act
		int result = post.getExp();
		// Assert
		assertEquals(0, result);
	}

	@Test
	@Tag("integration")
	public void getExpValueMatchesToStringOutput() {
		// Arrange
		Post post = new Post();
		post.setExp(8);
		// Act
		int result = post.getExp();
		String postString = post.toString();
		// Assert
		assertTrue(postString.contains("exp=8"));
		assertEquals(8, result);
	}

}