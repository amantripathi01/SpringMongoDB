
// ********RoostGPT********
/*
Test generated by RoostGPT for test test-name-pvt1 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=getExp_a80bcda041
ROOST_METHOD_SIG_HASH=getExp_e90c52dc7e

Scenario 1: Check if getExp() returns correct default value after Post object creation

Details:
  TestName: checkGetExpDefaultReturnValue.
  Description: This test is designed to confirm that the getExp() method should return its default value (null or 0) when a Post object is created with no modifications to the 'exp' field.
Execution:
  Arrange: Create a new Post object without setting 'exp'.
  Act: Invoke the getExp() method from the Post object.
  Assert: Assert that the return value is equal to the default value of int (0).
Validation:
  The 'exp' field should default to the int's default value (0) when not set, showing correct initial Post object creation behavior.

Scenario 2: Check if getExp() returns correct value after setting 'exp' value in Post object

Details:
  TestName: checkGetExpReturnValueAfterSettingValue.
  Description: The test is meant to check whether the getExp() method correctly returns the 'exp' variable's value set by the setExp() method.
Execution:
  Arrange: Create a new Post object and set a value to 'exp' through the setExp() method.
  Act: Invoke the getExp() method from the Post object.
  Assert: Assert that the return value is equal to the value previously set via setExp().
Validation:
  The getter method getExp() should return the correct 'exp' value after it was set, ensuring correct Post object behavior when 'exp' is modified.

Scenario 3: Check if multiple calls to getExp() consistently returns the same value

Details:
  TestName: checkGetExpConsistency.
  Description: The test is meant to validate whether multiple calls to getExp() consistently return the same 'exp' value, assuming 'exp' wasn't modified in between calls.
Execution:
  Arrange: Create a new Post object and set a value to 'exp' through the setExp() method.
  Act: Invoke the getExp() method multiple times from the Post object.
  Assert: Assert that the return value for each call is equal and matches the 'exp' value previously set.
Validation:
  It is essential to establish that getExp() is idempotent, returning the same result across multiple calls without changing the system's state, which is a good practice for getter methods.
*/

// ********RoostGPT********

package com.telusko.joblisting.model;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.data.mongodb.core.mapping.Document;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.*;
import java.util.Arrays;

@Document("Post")
public class PostGetExpTest {

	@Test
	@Tag("valid")
	public void checkGetExpDefaultReturnValue() {
		// Arrange
		Post post = new Post();
		// Act
		int exp = post.getExp();
		// Assert
		assertEquals(0, exp, "getExp() defaults to int default value of 0 on Post object creation");
	}

	@Test
	@Tag("valid")
	public void checkGetExpReturnValueAfterSettingValue() {
		// Arrange
		Post post = new Post();
		post.setExp(2);
		// Act
		int exp = post.getExp();
		// Assert
		assertEquals(2, exp, "getExp() returns the correct 'exp' value after it was set via setExp()");
	}

	@Test
	@Tag("valid")
	public void checkGetExpConsistency() {
		// Arrange
		Post post = new Post();
		post.setExp(3);
		// Act & Assert
		assertEquals(3, post.getExp(), "getExp() returns the same 'exp' value on first call");
		assertEquals(3, post.getExp(), "getExp() returns the same 'exp' value on second call");
		assertEquals(3, post.getExp(), "getExp() returns the same 'exp' value on third call");
	}

}