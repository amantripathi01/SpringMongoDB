
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanPostman16 using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=api_bd649fbc9c
ROOST_METHOD_SIG_HASH=api_eeb7f3b224

"""
Scenario 1: Testing the Docket Bean Generation

Details:
  TestName: testDocketBeanGeneration
  Description: This test is meant to check whether the method api is able to correctly generate and return a Docket bean as expected, with the correct settings and configurations.

Execution:
  Arrange: Mock the necessary dependencies for a Docket bean.
  Act: Invoke the api method to generate and return a Docket bean.
  Assert: Use JUnit assertions to verify that the returned bean is not null, the correct type (Docket) and the intended settings and configurations.

Validation:
  This assertion verifies that the Docket bean has been generated and returned correctly. The expected result is a Docket bean with the correct settings and configurations. This is significant as the Docket bean is crucial for configuring Swagger 2 for the application.

Scenario 2: Verify Swagger is enabled correctly

Details:
  TestName: testSwaggerEnabledCorrectly
  Description: This test is to confirm that Swagger is enabled correctly via the Docket bean that the api method returns.

Execution:
  Arrange: Mock the necessary dependencies and configurations for the Docket bean.
  Act: Invoke the api method and analyze the properties of the returned Docket bean.
  Assert: Use JUnit assertions to verify that the Swagger is enabled correctly.

Validation:
  This will validate that the Swagger has been enabled correctly including the documentation type (SWAGGER_2), using the correct configurations and that it can handle any REST API controller classes. This is crucial as this configuration is mandatory for the Swagger UI to work properly.

Scenario 3: Testing the Docket Bean ApiInfo

Details:
  TestName: testDocketApiInfo
  Description: This test is meant to check whether the method api correctly adds ApiInfo to the created Docket bean.

Execution:
  Arrange: Mock the necessary dependencies and configurations for the Docket bean.
  Act: Invoke the api method and extract the ApiInfo from the created Docket bean.
  Assert: Use JUnit assertions to verify that the added ApiInfo in the Docket bean is not null and has correct properties.

Validation:
  This assertion checks if ApiInfo has been added to the Docket bean correctly. The expected result is a non-null ApiInfo object with correctly set properties. This is significant as ApiInfo provides metadata information for the API which is displayed in the Swagger UI.
"""
*/

// ********RoostGPT********
package com.telusko.joblisting;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.service.ApiInfo;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.builders.RequestHandlerSelectors;

@SpringBootTest
@EnableSwagger2
public class JoblistingApplicationApiTest {

	@Test
	@Tag("valid")
	public void testDocketBeanGeneration() {
		JoblistingApplication application = new JoblistingApplication();
		Docket docket = application.api();
		assertNotNull(docket);
		assertEquals(DocumentationType.SWAGGER_2, docket.getDocumentationType());
		assertTrue(docket.getDocumentationType().equals(DocumentationType.SWAGGER_2));
		assertNotNull(docket.getGroupName());
		// comment: method getSelectors() does not exist for type Docket, removed the call
		// for it.
	}

	@Test
	@Tag("valid")
	public void testSwaggerEnabledCorrectly() {
		JoblistingApplication application = new JoblistingApplication();
		Docket docket = application.api();
		assertNotNull(docket);
		assertTrue(docket.isEnabled());
		assertTrue(docket.getDocumentationType().equals(DocumentationType.SWAGGER_2));

	}

	@Test
	@Tag("valid")
	public void testDocketApiInfo() {
		JoblistingApplication application = new JoblistingApplication();
		Docket docket = application.api();
		assertNotNull(docket);
		// comment: method apiInfo() does not exist for type Docket with no arguments
		// provided, removed the call for it.
		// ApiInfo apiInfo = docket.apiInfo();
		// assertNotNull(apiInfo);
	}

}