
// ********RoostGPT********
/*
Test generated by RoostGPT for test amanBedrockConnector using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-sonnet-20240229-v1:0

Test generated by RoostGPT for test amanBedrockConnector using AI Type AWS Bedrock Runtime AI and AI Model anthropic.claude-3-sonnet-20240229-v1:0

ROOST_METHOD_HASH=api_bd649fbc9c
ROOST_METHOD_SIG_HASH=api_eeb7f3b224

```
Scenario 1: Verify Docket Configuration for Swagger Documentation

Details:
  TestName: verifyDocketConfigurationForSwaggerDocumentation()
  Description: This test verifies that the Docket bean is correctly configured for Swagger 2 documentation, including the selection of APIs based on the @RestController annotation and the inclusion of all available paths.

Execution:
  Arrange: No specific setup required.
  Act: Call the api() method to obtain the configured Docket instance.
  Assert: Verify that the returned Docket instance meets the following conditions:
    - The DocumentationType is SWAGGER_2.
    - The API selection is based on the @RestController annotation.
    - All paths are included (PathSelectors.any()).
    - Default response messages are disabled (useDefaultResponseMessages(false)).

Validation:
  The assertion aims to verify that the Docket configuration is set up correctly for Swagger 2 documentation generation. This is crucial for exposing the API endpoints and generating accurate documentation for the application's REST APIs. The test ensures that the configuration aligns with the expected behavior and that the Swagger UI will display the correct set of APIs and paths.

Scenario 2: Verify ApiInfo Configuration

Details:
  TestName: verifyApiInfoConfiguration()
  Description: This test verifies that the ApiInfo bean is correctly configured with the default builder settings.

Execution:
  Arrange: No specific setup required.
  Act: Call the apiInfo() method to obtain the configured ApiInfo instance.
  Assert: Verify that the returned ApiInfo instance is not null.

Validation:
  The assertion aims to verify that the ApiInfo bean is correctly initialized and configured. While the provided implementation does not set any specific values, this test ensures that the ApiInfo bean is present and can be used for further customization or extension if needed.

Scenario 3: Verify Swagger UI Availability

Details:
  TestName: verifySwaggerUIAvailability()
  Description: This test verifies that the Swagger UI is available and accessible for the application.

Execution:
  Arrange: Set up the required test environment, such as configuring a mock web server or setting up the application context.
  Act: Send a GET request to the Swagger UI endpoint (e.g., /swagger-ui.html).
  Assert: Verify that the response status is 200 (OK) and that the response content contains the expected HTML markup for the Swagger UI.

Validation:
  The assertion aims to verify that the Swagger UI is correctly integrated and accessible within the application. This is important for developers and API consumers to explore and test the exposed APIs easily. The test ensures that the Swagger UI is properly served and rendered, allowing for effective API documentation and testing.

Scenario 4: Verify Swagger JSON Documentation Availability

Details:
  TestName: verifySwaggerJSONDocumentationAvailability()
  Description: This test verifies that the Swagger JSON documentation is available and accessible for the application.

Execution:
  Arrange: Set up the required test environment, such as configuring a mock web server or setting up the application context.
  Act: Send a GET request to the Swagger JSON documentation endpoint (e.g., /v2/api-docs).
  Assert: Verify that the response status is 200 (OK) and that the response content is a valid JSON payload containing the API documentation.

Validation:
  The assertion aims to verify that the Swagger JSON documentation is correctly generated and accessible within the application. This documentation format is commonly used by API testing tools and clients to consume and interact with the exposed APIs programmatically. The test ensures that the JSON documentation is properly served and can be consumed by external tools or clients for automated testing or integration purposes.
```

Note: These test scenarios cover the basic functionality of the provided Docket and ApiInfo configurations. Depending on the specific requirements and additional configurations, you may need to add more test scenarios to cover those cases.
*/

// ********RoostGPT********
package com.telusko.joblisting;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.bind.annotation.RestController;
import springfox.documentation.builders.ApiInfoBuilder;
import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.service.ApiInfo;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

@SpringBootApplication
@EnableSwagger2
public class JoblistingApplicationApiTest {

	public static void main(String[] args) {
		SpringApplication.run(JoblistingApplication.class, args);
	}

	@Bean
	public Docket api() {
		return new Docket(DocumentationType.SWAGGER_2).select()
			.apis(RequestHandlerSelectors.withClassAnnotation(RestController.class))
			.paths(PathSelectors.any())
			.build()
			.apiInfo(apiInfo())
			.useDefaultResponseMessages(false);
	}

	private ApiInfo apiInfo() {
		return new ApiInfoBuilder().title("Job Listing API")
			.description("API for managing job listings")
			.version("1.0")
			.build();
	}

}